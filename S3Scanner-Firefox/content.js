// content.js
// This script is injected into pages but we primarily use background webRequest for detection.
// We can use this to detect S3 links in the DOM if we wanted to be more aggressive.
console.log('S3Scanner content script loaded.');

{
  "name": "s3scanner-firefox",
  "version": "1.0.0",
  "description": "Firefox extension to detect public S3 buckets",
  "scripts": {
    "test": "jest",
    "lint": "eslint .",
    "format": "prettier --write .",
    "build": "echo 'No build step required. Load unpacked.'",
    "pack": "zip -r s3scanner-firefox.xpi ."
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "jest": "^29.0.0",
    "prettier": "^3.0.0",
    "jsdom": "^22.1.0"
  }
}
